apiVersion: v1
kind: ConfigMap
metadata:
  name: transcendence-config
  namespace: {{ .Release.Namespace }}
data:
{{- range $key, $val := .Values.configMap }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
{{- range $key, $val := .Values.configMap }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
